# Copy to .env and fill in. Never commit .env.

# Optional: PostgreSQL (later weeks / docker-compose)
DATABASE_URL=postgresql://bootcamp:bootcamp_secret@localhost:5432/rag_bootcamp

# Optional: Redis (caching / Celery)
REDIS_URL=redis://localhost:6379/0

# Required for LLM labs (Week 7+): get a key from https://platform.openai.com/
OPENAI_API_KEY=
